(function ( a ) {
    a.fn.enlarge = function ( c , d , f , e ) {
        if ( ! a( this ).data( "size" ) ) {
            a( this ).data( "size" , a( this ).size() )
        }
        if ( typeof c == "string" || typeof c == "number" ) {
            f = e;
            d = f;
            c = d;
            c = a( this ).data( "size" )
        }
        var b = {width : c.width , height : c.height , left : a( this ).position().left + a( this ).width() / 2 - c.width / 2 , top : a( this ).position().top + a( this ).height() / 2 - c.height / 2};
        a( this ).animate( b , d , f , e );
        return this
    };
    a.fn.enlargeIn = function ( b , c , e , d ) {
        return a( this ).attr( "data-status" , "enlargeIn" ).enlarge( b , c , e , d )
    };
    a.fn.enlargeOut = function ( b , d , c ) {
        a( this ).attr( "data-status" , "enlargeOut" ).enlarge( b , d , c )
    };
    a.fn.enlargeToggle = function ( c , d , f , e ) {
        var b = a( this ).attr( "data-status" );
        if ( ! b || b == "enlargeIn" ) {
            a( this ).enlargeOut( d , f , e )
        }
        else {
            a( this ).enlargeIn( c , d , f , e )
        }
    }
})( jQuery );