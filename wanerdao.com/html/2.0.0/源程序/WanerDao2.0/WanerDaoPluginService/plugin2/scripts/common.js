$(function () {
    //    parent.document.all("popFrame").style.height = document.body.scrollHeight;
    //    parent.document.all("popFrame").style.width = document.body.scrollWidth;
    $("#popFrame", parent.document).css("height", document.body.scrollHeight).css("width", document.body.scrollWidth);
});