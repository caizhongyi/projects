/*
依赖:
<script type="text/javascript" src="../js/jquery/jquery-1.5.js"></script>
*/
;(function ($) {

    $.fn.showPicToolBar = function (options) {
	
        var self=this;   //对像必须有唯一父级层
        var defaults=
        {
             //属性
        };

        if (options) {
            $.extend(defaults, options);
        };
        
		//代码
       
        return this;
    }


})(jQuery);