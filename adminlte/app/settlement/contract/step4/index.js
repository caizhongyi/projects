angular.module('settlement.contract.step4', [
    'settlement.contract.step4.service',
    'settlement.utils.service'
])
    .controller('controller.settlement.contract.step4',
    ["$scope", "$rootScope", "$stateParams", "$modal", "$state",  "settlement" ,
        function ($scope, $rootScope, $stateParams, $modal, $state,  settlement ) {

            $scope.save = function(){

            }

        }])

